class TeacherModel {
  constructor(id, email, password, firstName, lastName, competences) {
    this.id = id;
    this.email = email;
    this.password = password;
    this.firstName = firstName;
    this.lastName = lastName;
    this.competences = competences;
  }
}

export default TeacherModel;
